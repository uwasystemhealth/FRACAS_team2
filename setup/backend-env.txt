# Change the values below to your own values,
# Tokens, Security keys and password salt will be added when app is launched
# KEEP setup/.env SECURE

# MAIL PROVIDER for sending forget password and sign up requests
FRONTEND_URI="<DOMAIN>"
BACKEND_URI="<DOMAIN>"
MAIL_USERNAME="<EMAIL_ACCOUNT>"
MAIL_PASSWORD="<EMAIL_PASSWORD>"
MAIL_SERVER="<EMAIL_SERVER>"
MAIL_PORT=587