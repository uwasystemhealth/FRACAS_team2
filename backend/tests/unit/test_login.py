# User with no JWT token cannot access secure page

# Check login with user that doesn't exist

# Check login with wrong password

# Check login with correct password - JWT token and refresh token

# Test JWT token and refresh token can log into secure page

# Test JWT token expiry ?


# Placeholder test case
def test_1(user_data,record_data):
    assert True
